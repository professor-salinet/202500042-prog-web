create database `salinet`;
# a linha acima serve para criar um banco de dados com o nome: `salinet`
# o uso da crase se torna necessário para indicar uma `string` do sistema
# `strings` do sistema: `database`, `table` e `campo`